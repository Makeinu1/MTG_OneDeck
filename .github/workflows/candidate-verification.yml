name: Candidate Verification

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: candidate-verification-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  verify:
    if: github.repository == 'Makeinu1/MTG_OneDeck'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Full release candidate verification
        run: npm run check:release -- --base "origin/${{ github.base_ref }}" --head HEAD --build-base=/MTG_OneDeck/
