---
description: 要望を一つの検証可能な成果として開始する
---

`$ARGUMENTS`を一つの Goal として扱い、`.agents/skills/mtg-onedeck-development/SKILL.md`
に従う。開始時に `git status` と `HEAD`、該当契約、Done when を確認し、変更に直接関係する
テストを実行する。commit、push、deploy、publish は依頼で明示された場合だけ行う。
