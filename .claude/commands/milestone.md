---
description: 要望を1つの検証可能なマイルストーンとして開始する
---

`$ARGUMENTS`を1マイルストーンとして扱い、`.agents/skills/mtg-onedeck-development/SKILL.md`と`references/cycle.md`に従う。

開始時に`npm run codex:context -- [--domain <id>]`を実行し、milestone id、base SHA、Goal、Constraints、Done when、defer境界を凍結する。過去タスク全文を引き継がず、実装者にはタスク固有ブリーフのパスだけを渡す。
