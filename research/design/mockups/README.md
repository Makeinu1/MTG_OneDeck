# デザイン刷新モックアップ(視覚正本)

`index.html` = D-スライス実装(D1〜D4)の視覚基準。自己完結の静的HTML(依存なし)。

- **閲覧**: dev サーバー起動中なら http://localhost:5173/research/design/mockups/index.html 。ブラウザで直接 `index.html` を開いても可。スマホ実寸で見る場合は DevTools のデバイスモード(375×812)か、公開済み Artifact をスマホで開く。
- **4画面**: 上部タブで切替。①縦持ちゲーム画面 ②カードアクションシート(《森》の例=旧15項目メニューの後継) ③フィード+スタック展開 ④デスクトップ3カラム。画面内のカード/ベル/バックドロップのタップでも遷移する。
- **注釈**: 水色の吹き出しはモック専用の設計注釈(右下の「注釈 ON/OFF」で消せる)。製品には含めない。
- **規範**: `<style>` 冒頭のトークン層は `docs/design-system.md` §2-§7 の実体化。実装時はこのトークン定義を `src/index.css` に移植する(D1)。カードは CSS 描画のプレースホルダ(実装では Scryfall 画像)。
- **モック専用要素**: `.chrome`(画面切替タブ)・`.note`(注釈)・`.notes-toggle`・`data-go` 遷移スクリプトは製品に持ち込まない。

設計判断の根拠は `docs/design-vision.md`(診断・6原則・IA)、実装分解は `docs/ui-architecture-v2.md`。

`ambient-motion.html` = **生きた背景の視覚正本**(2026-07-20 ユーザー裁定・design-system §7 L0/§8/§8a・**v4.3 最終確定**)。4場面: ①ダーク・脈動する星雲(星雲ガス300s旋回+156星の個別きらめき+流れ星+鼓動700ms・戦闘/スタック/速度シミュレーション)②ライト・墨の世界(液态呼吸3400msアンカー+自描筆致+墨の滴+盤面上の光源90s公転)③ターン交代(スウィープ+スタンプ)+フェイズ(既存 TransitionCue)④reduced-motion 比較。ブラウザで直接開いて確認する(自己完結・依存なし・カードは Scryfall 実画像)。
