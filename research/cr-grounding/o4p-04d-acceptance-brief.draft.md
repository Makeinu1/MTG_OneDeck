# O4P-04D judge-owned acceptance brief

Milestone: `O4P-04D`

Base SHA: `1f6a465b859ba64c9961c6fcdae80087e33b9882`

Authority:
`research/cr-grounding/o4p-04d-guided-manual-actions.contract.draft.md`

The Judge owns this brief and every `review.*` assertion. The implementer must
not edit them.

## Required executable scenarios

1. Build a fresh deep-frozen view from one valid Player projection. Assert the
   exact own/revision/turn facts, ordered active Players, eligible own Search
   sessions/candidates, public control/combat candidates, generic face-down
   items, and manual correction inputs without hidden identity or physical IDs.
2. Reject Table/Spectator, wrong seat/presence/outcome/player/revision
   relations, shared synthetic-object drift, getters/descriptors/Proxies,
   hostile prototypes, sparse arrays, and same-reference mutation. DOM and
   errors remain the single generic Japanese unavailable state.
3. Create every guided action and manual-only action. Assert exact closed
   fields, fresh deep freeze, source non-mutation, projected-order preservation,
   exact untrimmed text, candidate membership, unique search choices, and
   rejection of unknown/symbol/accessor fields.
4. Bind search, control, attack, and block to exact validator-accepted command
   envelopes at `revision + 1`, with the required actor, decision maker,
   decision context, and payload. Reject session/action/revision/player/
   candidate/command-ID/bearer drift and every manual-only action with one
   fixed secret-free error.
5. Render five Japanese sections. Require native keyboard forms, explicit
   confirmation for guided command attempts, `サーバーへ確認する` wording, and
   no legality or acknowledgement claim.
6. Require `手動記録（未送信）` on face-down and correction paths before and
   after emission. Assert no digest/physical-ID field and no automatic/applied
   wording. Manual notes/reasons never appear in a DOM attribute or error.
7. Updating/offline disables emission while preserving the boundary text.
   Projection/revision change removes stale choices and confirmation. Pairing
   failure removes Workbench, Guided Actions, and Table children.
8. Architecture evidence permits only the four public barrels in the pure
   module and exact React composition. It rejects Store/Solo/GameScreen,
   private Core/Room/protocol/projection files, Cloudflare/headless, network,
   storage, timer/RNG, root App integration, dependency/config/version changes,
   and unregistered Online roots.
9. In one stable browser session inspect 375x812, 812x375, and 1440x900.
   Assert horizontal overflow zero, no fixed overlay, all five families and
   paired surfaces reachable, focus visible, and console errors zero.
10. Run affected checks only before freeze. After independent BROAD audit has
    BLOCKER/HIGH zero, run one `npm run check` on the same release fingerprint.

## Judge-owned evidence paths

- `src/components/online/__tests__/review.o4p-04d-guided-actions.test.tsx`
- `src/test/architecture/review.o4p-04d-guided-actions-boundary.test.ts`

These files and the contract/audit evidence are outside implementer write
scope.
