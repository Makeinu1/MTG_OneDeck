# O4P-09 Roadmap Registration Acceptance

Date: 2026-08-25
Authority: user-ruling-2026-08-25-shared-chat
Base SHA: `629de59eb244e6c9eeb78c3bdab29cfd15596b48`

1. `goalPolicy.activeProgram` is exactly `O4P-09` with ordered IDs A through J.
2. A-J exist exactly once in `domains` and `plannedSequence`, share every
   semantic field, start `pending`, and use `crOrder` 1033 through 1042.
3. A depends on shipped O4P-08D; every later item depends only on its immediate
   O4P-09 predecessor.
4. Every pre-existing ledger entry and status is byte-equivalent to the base;
   only the exact active program and ten appended pairs change.
5. `GOV-CODEX-56R2-2026-08` remains `audited` and is neither shipped nor made a
   product prerequisite.
6. The roadmap freezes the single GameScreen and Core path, Pregame, safe
   tabletop primitives, enforced visibility, assisted priority/HOLD/steward,
   steward-only voice-consensus UNDO, combat/manual fallback, shared
   checkpoints, full-match E2E, and separate secret-safe spectator surface.
7. A prepares the shared GameScreen seam; B owns Local/Remote application
   wiring. D does not enable hidden-zone access; E owns Look/Reveal/Choose.
   F owns the undo authority predicate; H owns shared rollback state.
8. Registration changes only Judge-owned ledger, roadmap evidence, review, and
   exact historical active-program guards. Product/runtime/configuration,
   dependencies, CR pin, Worker, and Pages behavior remain unchanged.
9. `npm run codex:context -- --domain O4P-09A` reports healthy explicit
   selection, active status, and `nextDomainId: O4P-09A`.
10. A fresh-context Sol/high R3/BROAD audit reports BLOCKER/HIGH zero before the
    candidate full check. Registration itself is not implementation or ship
    evidence.

