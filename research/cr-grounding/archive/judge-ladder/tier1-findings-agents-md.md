# Tier-1 監査所見: AGENTS.md 全面書き換え + 関連ファイル整合

- 監査対象: 未コミット working tree diff(`AGENTS.md` / `CLAUDE.md` / `.claude/commands/{audit,autoloop,milestone}.md`)
- 実行者: 冷たい Tier-1 セッション(実装文脈なし、`.claude/commands/audit.md` Tier-1 手順準拠)
- 実行日: 2026-07-02
- 方式: findings only。契約・ファイルは一切変更していない。git add/commit なし。

## A. 変更ファイル走査

`git diff --name-only` 結果:

```
.claude/commands/audit.md
.claude/commands/autoloop.md
.claude/commands/milestone.md
AGENTS.md
CLAUDE.md
```

前提どおり5ファイルのみ。`src/engine/` ・`review.*` ・`docs/` ・`eslint.config.js` ・`CACHE_SCHEMA_VERSION` への変更なし。

**判定: PASS**(想定外ファイルなし)

## B. 機械チェック4点(各個実行)

| チェック | 結果 |
|---|---|
| `npm run lint` | PASS(エラー・警告出力なし) |
| `npx tsc --noEmit` | PASS(出力なし=型エラーなし) |
| `npx vitest run` | PASS — 102 test files / 1102 tests all passed |
| `npm run build` | PASS — `tsc -b && vite build` 成功、`dist/` 生成確認後に削除済み |

**判定: PASS**(4点すべて通過。ドキュメント変更のみで挙動非破壊を確認)

## C. 統制 weakening の敵対検査(逐条)

### C-1. Codex の git 操作禁止(常時・J0でも)
AGENTS.md「不可侵」節(13行目)に明記、J0節(21行目)でも「git・`CLAUDE.md`・本ファイルは不可のまま」と明記。CLAUDE.md 39行目「実装エージェント(Codex 含む)の git 操作は禁止」とも整合。

**判定: PASS**

### C-2. `review.*` 変更禁止(判定者在席時)+ J0 例外条件が CLAUDE.md「役割は能力で定義する」節と整合
AGENTS.md「不可侵」節(14行目)で `review.*` 変更禁止を明記。J0節(21行目)「ユーザーが『判定者不在・あなたが批評+実装を両担してよい』と明示した時のみ、`review.*`/`docs/` の直接変更が許される」「条件=復帰した判定者が CR 等の外部権威に当てて独立再検証・再オーナー化するまで、あなたの緑は『未監査』扱い」。

CLAUDE.md 29行目(役割は能力で定義する節)は「判定者不在中に Codex が `review.*`/`docs/` を直接書いた場合も違反ではない——条件は『戻った判定者が CR 等の外部権威に当てて独立再検証し再オーナー化する』こと」とほぼ同一文言。矛盾なし。

軽微な差異: AGENTS.md の J0 発動条件は「ユーザーが明示した時のみ」という追加要件を課しており、CLAUDE.md 側は「判定者が不在(Claude のトークン枯渇等)の時は」という事実状態のみで発動としている。AGENTS.md がより厳格(安全側)な条件を追加しているだけで、統制の弱化ではなく強化。

**判定: PASS**(矛盾なし。AGENTS.md 側がむしろ安全側に厳格化)

### C-3. `CLAUDE.md`/`AGENTS.md`/`eslint.config.js`/`CACHE_SCHEMA_VERSION` の変更禁止
AGENTS.md「不可侵」節(15-16行目)に4項目とも明記。`.claude/commands/audit.md` の Tier-1 走査対象リスト(11行目)も `AGENTS.md` を追加済み(旧版は未掲載だった禁止ファイルリストに `AGENTS.md` が新規追加されている=検出漏れの是正)。

一点、赤旗候補として記録: **CLAUDE.md 本体**の「役割は能力で定義する」節末尾(55行目)は「`CLAUDE.md` は在席する最上位の Claude 判定者専有(判定者不在時でも Codex は触らない)」と不可侵ファイルを明記する唯一の箇所だが、**この55行目に `AGENTS.md` が追加されていない**(`CLAUDE.md` のみ言及)。CLAUDE.md 37行目に「常設の実装者規約=`AGENTS.md`(...判定者専有=Codex は従うが編集不可)」という言及はあるが、これは「役割分担」の説明文脈であり、55行目のような明示的な不可侵ファイル列挙とは書式が異なる。AGENTS.md 自身・audit.md・milestone.md・autoloop.md はいずれも `AGENTS.md` を不可侵/変更禁止として扱っているため実害は無いと判断するが、**正本(CLAUDE.md)側の不可侵ファイル列挙にAGENTS.mdが統一的に追加されていない**のは是正余地あり。

**判定: 赤旗(軽微)** — CLAUDE.md 55行目の不可侵ファイル列挙に `AGENTS.md` が明記されていない。実害は他ファイルの記述で相殺されているが、単一正本原則からのズレとして Tier-2 裁定を推奨。

### C-4. 独立監査の要石(実装者≠受け入れ基準作者)が弱められていないか
AGENTS.md 7行目「判定者=...契約の承認・独立監査・git commit/push を担う」、報告様式節(54行目)「あなたの『テスト通過』自己申告は合否判定に使われない(別セッションの独立監査が敵対的に検証する)」と明記。milestone.md 20行目「レビュー専有テスト `review.<key>` の最終 author は判定者(=要石。実装者≠受け入れ基準作者)」は変更なし(diff対象外)で維持。CLAUDE.md 29行目の要石文言も維持。

**判定: PASS**(要石は全経路で保持)

### C-5. 機械チェック4点・報告様式が CLAUDE.md 検証プロトコルと整合するか
AGENTS.md 48行目「機械チェック4点を各個に実行し全通過(`&&` 連結しない): npm run lint / npx tsc --noEmit / npx vitest run / npm run build」は CLAUDE.md 82行目の記述と一致(コマンド・順序とも同一)。`dist/` 削除の指示(49行目)も一致。

軽微所見: 旧 AGENTS.md は「検証: 機械チェック4点 **とブラウザ実機確認(コンソールエラー0件)**」を実装エージェント向け要約に含めていたが、新 AGENTS.md の「受け入れ標準」節(46-50行目)は機械チェック4点のみで、ブラウザ実機確認・コンソールエラー0件への言及が消えている。CLAUDE.md 12行目は「実機ブラウザ確認は要所の UI だけ**判定者**が行う」、79行目は「UI変更はブラウザ実機で確認。コンソールエラー0件が合格条件」で主体を限定していない一般規範。Codex CLI はブラウザプレビューツールを保有しないため実装者責務ではなかった可能性もあるが、旧版が実装エージェント向け要約にこの項目を含めていた経緯を踏まえると、**受け入れ基準からの参照が新版で欠落**している。

**判定: 赤旗(要 Tier-2 裁定)** — ブラウザ実機確認/コンソールエラー0件が AGENTS.md の受け入れ標準から欠落。実装者の責務だったか判定者専属だったか曖昧なため、意図的スコープ整理か weakening か切り分けが必要。

### C-6. ブリーフ短縮がタスク固有ゲートを消していないか
milestone.md 19行目「含めるのは**タスク固有のみ**: 4行ヘッダ・目的1行・契約参照・**スコープ境界(defer/隔離の明示)**・対象ファイル・golden ケース・**タスク固有の受け入れ条件**・(あれば)AGENTS.md からの逸脱指示」と明記。4行ヘッダ自体(milestone.md 9-13行目)にも「3. 残す未実装境界: 明示的に defer する範囲」が含まれる。共通則(不可侵リスト・エンジン規律・機械4点・報告様式)のみが AGENTS.md へ移管され、タスク固有の scope/defer/受け入れ条件はブリーフに残す設計が明示的に保証されている。

**判定: PASS**

## D. AGENTS.md 単体の初見可読性

- ファイルサイズ: 5714 バイト(< 32 KiB)。**PASS**
- 構成: 役割説明 → 不可侵 → J0 → エンジン規律 → CR接地 → コーディング規約 → 受け入れ標準 → 報告様式。実行順に近い自然な流れで、初見の実装エージェントが迷う構造的曖昧さは見当たらない。
- 優先順位節(9行目)「CLAUDE.md > ブリーフ > 本ファイル」は明確。矛盾時の解決規則があり実行不能な指示はない。
- 軽微な不正確箇所: 27行目「不変条件 I1〜I7・I13〜I16」は engine-spec.md 上に実在する番号だが(1842-1847行に I13〜I16 定義を確認)、**I8・I9 も実在するのに列挙から漏れている**(engine-spec.md 1543行 I8、1739行 I9)。これは CLAUDE.md 77行目の既存文言「I1〜I7」をそのまま踏襲し I13-I16 を追加しただけで、AGENTS.md 固有の新規劣化ではない。実害は軽微(単なる列挙漏れ、不変条件そのものの弱体化ではない)。

**判定: PASS**(サイズ・構造・自己矛盾なし。I8/I9 列挙漏れは軽微所見として記録するのみ)

## E. 旧版 AGENTS.md との実質統制比較(`git show HEAD:AGENTS.md`)

旧版(954バイト)は「正本は CLAUDE.md」型の跳ね返しドキュメントで、実質統制は以下のみ:
- 役割分担の要約(Fable=計画/契約/監査/レビュー/コミット、Codex=実装)
- 実装担当の禁止事項: git操作、`docs/` 変更、`review.*` 変更
- 契約の所在(engine-spec.md / acceptance.md)
- 検証: 機械チェック4点 + ブラウザ実機確認(コンソールエラー0件)

新版はこれらを全て保持した上で、J0モード・エンジン規律の具体化・CR接地・コーディング規約・報告様式を追加している。**欠落として確認したのは C-5 に記載したブラウザ実機確認/コンソールエラー0件の言及のみ**。`docs/acceptance.md` への直接参照も旧版にはあったが新版は `docs/engine-spec.md` のみ言及(ただし「不可侵」節の `docs/` 直接変更禁止は acceptance.md も包含するため実害なし)。

**判定: 赤旗(C-5 と同一事象)** — 実質的統制の欠落はブラウザ実機確認言及の1点のみ。他はすべて保持または強化。

## 総括

- **PASS 数: 8**(A, B, C-1, C-2, C-4, C-6, D, および C-3 は赤旗だが他小項目はPASS基調)
  - 内訳: A=PASS, B=PASS, C-1=PASS, C-2=PASS, C-3=赤旗(軽微), C-4=PASS, C-5=赤旗(要裁定), C-6=PASS, D=PASS, E=赤旗(C-5と同一事象)
- **赤旗一覧(Tier-2 裁定が必要な項目)**:
  1. **CLAUDE.md 55行目の不可侵ファイル列挙に `AGENTS.md` が明記されていない**(正本側の記述漏れ。実害は他ファイルの記述で相殺されているが単一正本原則の観点で是正余地あり)
  2. **AGENTS.md 新版の「受け入れ標準」節からブラウザ実機確認/コンソールエラー0件への言及が消えている**(旧版には実装エージェント向け要約として存在した。CLAUDE.md 側は主体を判定者に限定する文脈と一般規範が併存し曖昧。意図的スコープ整理か weakening か切り分けが必要)

いずれも contract weakening というより**参照漏れ・記述の非対称**の水準であり、統制の実質的崩壊は確認されなかった。git 操作禁止・review.*保護・独立監査要石・機械4点・ブリーフのタスク固有ゲート保持は全経路で確認済み。

---

## Tier-2 裁定(判定者・2026-07-02)

- 赤旗1(CLAUDE.md 不可侵列挙に AGENTS.md 欠落): **正当**。CLAUDE.md M0節の専有宣言を「`CLAUDE.md`・`AGENTS.md` は判定者専有」へ修正済み。
- 赤旗2(ブラウザ実機確認の欠落): **正当(部分)**。実機確認の主体は判定者(audit Tier-2)だが、実装者にも「UI変更タスクはコンソールエラー0件・新規エラーを残さない」を AGENTS.md 受け入れ標準へ追加済み。
- 軽微所見(I1〜I7・I13〜I16 の列挙漏れ): 番号列挙をやめ「engine-spec 定義済みの I 系列すべて」へ変更(将来ドリフト防止)。

3件とも監査者指摘の逐語適用(外科修正)。機械チェック4点は ship pre-push gate で再実行される。
