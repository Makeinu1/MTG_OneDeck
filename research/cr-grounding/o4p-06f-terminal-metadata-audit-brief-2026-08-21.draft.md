# O4P-06F Terminal Metadata Audit Brief

Milestone: `O4P-06F`
Base HEAD: `9a97da9c6af756a1988a85b21c872a3a2c9fa565`
Role: context-free findings-only terminal metadata auditor

Audit only:

- `research/cr-grounding/cr-backbone-ledger.json`;
- `research/cr-grounding/archive/o4p-06f-completion-packet-2026-08-21.md`;
- `src/test/architecture/review.o4p-06-roadmap-registration.test.ts`;
- this brief; and
- read-only local/GitHub/Pages/Cloudflare evidence named by those files.

Confirm both ledger collections contain exactly one O4P-06F entry, are
equivalent outside collection-specific `id`/`domainId`/`type`, and both change
only O4P-06F from pending registration to the same shipped evidence, nextGate,
judge, and note. Confirm O4P-06E remains shipped, every O4P-06A-F entry is
shipped, no other domain status/history changed, and active O4P-06 has no next
pending domain. No successor milestone may be created or started.

Confirm the sole historical roadmap-review repair preserves the pending-program
branch byte-for-byte in meaning and adds only the exact completed-program branch:
all A-F shipped, `status: complete`, `nextDomainId: null`, and no O4P-06
selection. Removing that branch must restore the prior test logic, and the old
unconditional pending assertion must fail non-vacuously on this terminal tree.

Verify every evidence path exists and every audit identity/fingerprint/count is
faithful to its immutable record. Verify semantic and evidence commits, Actions
`32473802443` and `32476785026` exact heads/outcomes/counts, ownership/build/
artifact/Pages steps, served HTML/JS/CSS names/status/Last-Modified, formal
summary SHA/facts, fixed Wrangler and only declared bindings, distinct retained
Worker versions, final post version active 100%, root/probe 404, private
same-Room status revision/count 5, and production evidence approval.

Reject secrets, Room/participant/capability/account/token/private-key material,
raw JSON/tail/frame evidence, a success claim for the discarded first attempt,
unrecorded product or policy changes, false 24-hour/account-wide claims, or any
new milestone start. Run only JSON/context/roadmap-review/diff/forbidden checks
plus read-only `gh`, `curl`, pinned Wrangler status/version, and private local
parsers needed for metadata. Never print private correlation values.

Do not edit, create records, run `npm run check`, mutate git, push, deploy,
delete resources, or access secrets. Return BLOCKER/HIGH/MEDIUM/LOW and
`O4P-06F-TERMINAL-METADATA-APPROVED` only if exact. Approval authorizes only
the terminal metadata commit/push and final exact-head CI/Pages/Worker smoke.
