# O4P-06F CI reauthorization cold-audit brief

Date: 2026-08-21
Milestone: `O4P-06F`
Candidate commit: `fc15775184d8c23c8193e0505c91018396591809`
Run: `32456019180`

Role: context-free Luna xhigh findings-only auditor. Do not edit, stage, run a
local full check, launch Chrome, deploy, publish, or mutate git. Read only this
brief, `research/cr-grounding/o4p-06f-ci-reauthorization-record.draft.md`, the
three prior audit records it names, and read-only local/GitHub evidence needed
to verify the claims.

Independently require:

1. local `HEAD == origin/main ==` the exact candidate and tracked worktree is
   clean before these two uncommitted metadata files;
2. run/job head identity, clean-checkout full-check success, Core/DOM totals,
   build success, machine total, and resolved base are exact;
3. ownership output is exactly 13 ordered `NEEDS-REAUTH` plus 4 ordered
   `FORBIDDEN` paths with no eighteenth path, and all 17 recorded SHA-256 values
   match candidate bytes;
4. Pages configure/artifact/deploy were skipped and the record makes no Pages,
   Worker, production-browser, or shipment claim;
5. product, repair, and recovery audit identities/fingerprints/counts remain
   applicable to the candidate commit; and
6. committing only these two metadata paths yields a parent-only metadata diff
   with no candidate implementation/review/verifier/package/policy/runtime
   change and permits the next exact-head CI to exercise normal ownership.

Report BLOCKER/HIGH/MEDIUM/LOW. Return
`O4P-06F-CI-REAUTHORIZATION-APPROVED` only with all counts zero.
