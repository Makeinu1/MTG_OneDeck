# GOV-CODEX-58A Acceptance

Base SHA: `74d24c0311e0d58112b15c58d6f8546449a5b01a`
Risk: R3 / BROAD

The Judge owns these criteria. One failure fails the scenario.

1. A synchronized GOV-CODEX-58A candidate at the declared clean base projects
   one `activeCandidate.state`, exact authority source, counters, roles,
   waits, and `permissionRequired`.
2. Candidate/domain/base/fingerprint/authority disagreement and more than one
   unshipped implementation candidate stop context and `codex:program-step`
   nonzero before audit or release.
3. A requested local-write/commit/push/deploy/ship action with false or
   untracked authority stops. Complete autonomy never changes a false bit.
4. `audit-failed-stop` cannot be renamed, reset, or advanced to release. Under
   complete autonomy, an exact supervisor repair-resume may return only to
   correction for a new bounded root cause while preserving acceptance,
   authority, history, and cumulative counters. It never asks for or accepts a
   numeric quota as authority.
5. `check:budget` rejects missing, decreasing, reset, negative, or mislabeled
   counters. Structural lineage/wait/initial-semantic-push excess remains
   nonzero. Valid cumulative replacement-push/full-check/token/cycle/correction/
   compaction/continuation watchdog crossings return explicit advisories and
   remain executable. Cached and total input remain separate terminal telemetry.
6. A second implementer or cold-auditor lineage, second audit or CI wait chain,
   or excess initial semantic push stops. An additional replacement push stays
   cumulative and advised, requires existing push authority and the ordinary
   same-acceptance audit/full-check/commit path, and never resets prior history.
   A third full-check attempt likewise remains counted and advised; neither can
   substitute for a final exact-tree green check. Numeric objectives never
   generate a human permission request.
7. `check:guard-impact` emits stable changed paths, ownership, exact
   path/import/allowlist guard references, predecessor-hash references,
   reauthorization paths, and a stable fingerprint.
8. Protected/guarded changes without an exact acknowledgement fail.
   Omitted/additional/wildcard/stale-fingerprint acknowledgement also fails;
   byte-exact acknowledgement passes.
9. `check:release-preflight` consumes the same supervisor, budget, permission,
   and guard-impact result and reports explicit check fields/error codes.
10. `codex:context`, `check:terminal-metadata`, `check:forbidden`, generated
    API validation, current governance unit tests, and the GOV-CODEX-56/57
    reviews remain green.
11. The existing skill invokes the three canonical commands at their required
    phases and retains the one-implementer, one-auditor, one-wait rules.
12. GOV-CODEX-58B topics remain absent from 58A product claims and O4P-09F
    product source remains unchanged.
13. Supervisor state has a tracked, hash-chained anchor outside ignored
    loop-state; truncation, rewrite, reset, forged repair origin, or STOP
    laundering fails. An exact user ruling may change outcome policy; otherwise
    only a same-scope supervisor repair-resume may leave STOP.
14. Gated actions require verified session/usage receipts, guard validation is
    intrinsic to the transition, guard fingerprints include current byte hashes
    and base/current references, and an explicit pre-enforcement domain cannot
    bypass the one active supervised candidate.
15. The frozen cold-audit envelope binds both the ordinary candidate tree
    fingerprint and the tracked authority's latest event hash, closing the
    intentional self-reference exclusion before the first commit.
16. Bootstrap and every gated mutation derive a current receipt atomically
    from the exact session/role plan. The first tracked event fixes the baseline
    and ordered session-role allowlist; shifted baseline, swapped role/session,
    omission, or addition fails without mutation. Long guard work or normal
    JSONL growth after the action boundary cannot create
    `STALE_USAGE_RECEIPT`; an external caller-selected stale prefix still fails,
    and every next action appends a newly derived monotonic receipt.
17. A tracked candidate with legitimate repository-byte drift remains stopped
    until supervisor-only `refresh-fingerprint` verifies the old chain, exact
    current bytes/guards/owner, unchanged acceptance and authority, and appends
    a new receipt-bound event. Direct drift and scope drift remain nonzero.
18. The successful canonical context command stays below 12 KiB without
    dropping any projection fields.
19. The canonical `runProgramStep` path reaches `derive-repair` from a verified
    `repair-required` record while malformed tracked authority still fails
    closed; pure state-helper coverage is insufficient.
20. The permanent contract states the requested outcome and human decision
    boundary, and the executable supervisor proves all-true candidate authority
    proceeds without repeated permission prompts.
21. A real semantic git commit may advance a `push-ready` candidate only when
    its immutable base is an ancestor, its HEAD/working semantic tree equals
    the audited fingerprint, and semantic drift is absent. Push recording binds
    current HEAD once as `releaseHeadSha`; later CI/deploy/ship rejects every
    other HEAD, including a same-tree metadata commit.
22. A repair candidate uses the current HEAD as its new immutable base while
    preserving acceptance, authority, cumulative counters, lineage, waits, and
    append-only history. `baseSha` is never overwritten merely to disguise a
    post-commit mismatch.
23. Only the exact active-domain supervisor event file can be a terminal
    append. The verifier proves the HEAD prefix, event/receipt/hash/counter/state
    chain and ledger agreement; truncation, rewrite, corruption, another domain,
    unreadable oversized HEAD authority, or any extra path fails closed. A
    valid append leaves the semantic fingerprint unchanged and changes the
    terminal fingerprint by its exact path and latest event hash. In CI for the
    terminal metadata commit, `releaseHeadSha` equals the already checked and
    deployed semantic diff base, while the successor HEAD may differ only by
    the verified terminal paths; it is never rebound to the metadata commit.
24. Offline receipt history is prefix-monotonic per verified session: byte
    lengths never decrease, and the hash at an identical byte length never
    changes. A confirmed missing predecessor is distinct from any probe/read
    execution or integrity failure, and both categories fail closed.
25. A clean checkout without ignored loop state recovers its active candidate
    only from the latest candidate in a valid tracked authority at HEAD, verifies
    the chain offline, and synthesizes matching milestone/state/base/tree fields.
    Every historical event candidate must also satisfy the full candidate shape,
    state-specific evidence, resolved supervision policy, acceptance fingerprint,
    and authority/source invariants after the entire chain is rehashed. Dirty,
    corrupt, absent, unreadable, rewritten, or mismatched authority stays nonzero;
    an existing loop state retains live receipt verification.
26. Default `check:forbidden -- --diff <base>` keeps Judge-owned `review.*`
    changes hard-red unless the active tracked supervisor authority and clean
    context prove the exact guard acknowledgement for every such path. No caller
    owner, allowlist, manifest, or acknowledgement input is accepted. Exact
    path/owner/base/current hashes/candidate/tree/semantic guards/predecessor
    hashes pass; omission, addition, wildcard, stale bytes, wrong base or
    candidate, corrupt/other-domain authority, or extra supervisor path fails.
