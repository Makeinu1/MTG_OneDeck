# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local research datasets (keep .md summaries + manifest.json; exclude raw and large machine blobs)
research/scryfall-rules/**/raw/
research/scryfall-rules/**/analysis/*.json
# exclude only the large per-card dump; keep known-divergences.json (registry)
research/classifier-accuracy/report.json
# grammar coverage: keep report.md summary; exclude large machine blob
research/grammar-coverage/report.json
# classifier parity: keep report.md summary; exclude large per-card dump (regenerable via npm run classifier-parity)
research/classifier-parity/report.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
