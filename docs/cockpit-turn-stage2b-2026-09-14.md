# 段階2B: 通常ターン進行とクリーンナップ

Refs #4, #5, #6, #9。基点はPR #23の55886931474bba2465382789752b361ae1974c57。

## 実施時点の状態（2026-09-14）
**本体修正はローカルのみ。GitHub上のこのブランチは仕様記録と通常UI試験スクリプトを保存するもので、修正済み製品ブランチではない。**
新しいCI workflowの作成は連携の安全確認で拒否された。別のworkflowへ迂回していない。適用前の一時転記パッチと、本体反映前では成立しない追加coreテストは、このターンに作成したものだけを撤回した。既存の本体・既存workflow・PR #22/#23・mainは変更していない。
ローカル最終確認はcore12ファイル25件、DOM/Client/実SQLite4ファイル26件、合計51件PASS。TypeScript/Vite build、変更範囲ESLint、diff checkもPASS。追加core2件は同じ試験を旧エンジンへ向けるとFAILとなることを確認。
**通常UIの二巡、今回の候補での実HTTP/workerd、前段の実ブラウザー復帰試験の再確認は未実施。** ローカルChromiumの接続試行は管理ポリシーによってnavigation時点で拒否され、プレイ操作を検証できていない。前段CIのPASSを今回の候補へ流用しない。
正しい完全差分・変更後ファイル・検証記録は依頼スレッドの段階2B成果物へ保存する。Issueの追加条件・独立レビュー・フルマッチは未完了。Issueを閉じない。

## Goal / acceptance
通常のボタン・進行キー・任意の開始一括支援を同じサーバー確定処理へ接続する。
アンタップ、通常ドロー、ステップ境界のマナ消去、終了時の手札選択・ダメージと明示期限の解除を、通常UIから二巡して保存結果で確認する。
通信復帰と戦闘/コピーの前段合格は再検査する。

## ローカル候補の仕様決定
- M-08の「個別進行」は、単なる位置変更ではなく、各ステップの定型処理を含む。任意shortcutも同じ境界処理を使用し、誘発で中断・再開する。
- 一人回しは初回ドローあり。2人で始まった対戦は先手の最初のドローステップ全体を省略する（CR 103.8a）。4人で開始した対戦は省略しない。人数減少でこの区別を変えない。
- アンタップ済みの事実を保存し、再接続や同じステップの再表示で二度処理しない。継続制御判定には各席の実際のターン開始を保存し、残席数から過去を逆算しない（CR 302.6）。
- M-11: 手札上限超過時は必要枚数を先に選ぶ。その後、全席の記録ダメージと明示的な「ターン終了まで」の修整/キーワードを解除する（CR 514.1–2）。任意の期限文章を推測せず、他の期間は解除/保持の明示確認を要求する。
- cleanupの部分調整と完了を区別し、未完のままturn命令で迂回しない。誘発や追加操作で完了状態を失効し、もう一度cleanupを通す（CR 514.3a）。
- 例外の手動補正は明示ボタンでのみ選び、その境界のアンタップ/ドロー/マナ処理を人が反映済みとして扱う。未完cleanup・HOLD・Stack・必須誘発を飛ばす権限にはしない。

## Non-goals / untouched
全Oracle、任意の置換・アンタップ制限・状況起因処理の自動裁定は追加しない。空の山札からの通常ドローは確認へ止め、人が置換/敗北を判断する。
未知の旧保存のターン履歴を捏造しない。履歴不明のマナ・クリーチャーは次の実ターン開始まで保守的に扱う。旧保存全形式の検証完了とはしない。
main・本番配備・依存/lockfile・秘密投影(#16)・既存一時workflowは変更しない。スマホのPC利用案内をスマホ対戦PASSと扱わない。

## 検証水準
追加core2件は通常ターン1/2/4席の二巡と誘発中断/再開・cleanup再確認を検査する。追加DOM1件は確認画面と入力ロックを検査する（本番対戦の代用ではない）。
後続の実HTTP/workerd/SQLite/独立ブラウザー試験ではsourceShaと合否を記録する。APIで準備する戦闘試験と通常UIのターン試験を区別する。fullMatchの完了はこの段階では認定しない。

一次参照: https://media.wizards.com/2026/downloads/MagicCompRules%2020260819.txt
